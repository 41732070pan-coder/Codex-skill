# 练习计划

## 实践类型

`code_experiment`

## 练习任务

执行或纸面模拟 `build` 与 `build --fast` 两个命令；运行前预测差异，再记录 `--fast` 对应的预期变化。

## 验收标准

- 能说明 `build` 是基础命令、`--fast` 是可选参数，并根据来源表格解释预期差异。
- 能指出练习使用的来源证据层级。

## 迁移练习

改变一个条件，预测结果，再说明哪些判断仍然成立。
